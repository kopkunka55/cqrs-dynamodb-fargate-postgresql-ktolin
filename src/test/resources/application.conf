ktor {
    deployment {
        port = 8081
    }
    application {
        modules = [ com.kopkunka55.ApplicationKt.testModule ]
    }
    aws {
        ddb_table = anywallet-dev
    }
    database {
        url = "localhost:5432"
        user = "dev"
        password = "password"
    }
}
