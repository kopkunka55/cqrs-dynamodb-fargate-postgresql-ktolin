ktor {
    deployment {
        port = 8081
        port = ${?PORT}
    }
    application {
        modules = [ com.kopkunka55.ApplicationKt.module ]
        mode = COMMAND
        mode = ${?CQRS_MODE}
    }
    aws {
        ddb_table = any-wallet-dev
    }
}
